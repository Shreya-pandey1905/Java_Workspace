import { CustomroleConversionPipe } from './customrole-conversion-pipe';

describe('CustomroleConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomroleConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
