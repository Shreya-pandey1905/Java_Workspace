import { CustomFullNPipe } from './custom-full-n-pipe';

describe('CustomFullNPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFullNPipe();
    expect(pipe).toBeTruthy();
  });
});
